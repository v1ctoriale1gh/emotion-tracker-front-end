import React, { Component } from 'react';
import { connect } from 'react-redux';
import { addLog } from '../actions';


class UserSignup extends Component {

    
}


export default connect(null, { addLog })(UserSignup);